{
  "name": "queuesmart-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "QueueSmart REST API (A3 back end) — in-memory, no database yet.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:setup": "node db/setup.js",
    "db:seed": "node db/seed.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --config jest.config.json --runInBand",
    "test:coverage": "node --experimental-vm-modules node_modules/jest/bin/jest.js --config jest.config.json --runInBand --coverage"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.23.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
